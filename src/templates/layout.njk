{# layout.njk #}

    {%- block main -%} {%- endblock %}
    <!-- You write code for this content block in another file -->
    <!-- The minus signs are for whitespace control -->

    <script src="https://code.jquery.com/jquery-3.2.1.min.js"></script>
    <script src="js/vendor/materialize.min.js"></script>
    <script>
      $(document).ready(function(){
        $('.nav-wrapper').pushpin({
          top: 0,
          bottom: 500,
          offset: 0
        });
        $('.parallax').parallax();

        var options = [
          {selector: '.scrollfire-test', offset: 200, callback: function(e) {Materialize.fadeInImage($(e))} }
        ];
        Materialize.scrollFire(options);
        $('.scrollspy').scrollSpy();
      });
    </script>
  </body>
</html>
